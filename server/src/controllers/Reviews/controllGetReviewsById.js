const {Products,Colors,Sizes,Categories,Series,Stocks, Reviews} = require("../../db");

const getReviewsById = async () => {
    
   const reviews = async (id) => {
  
    };
};

module.exports = getReviewsById;

